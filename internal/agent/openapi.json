{
  "openapi": "3.1.0",
  "info": {
    "title": "Universal VPS Root Shell",
    "version": "0.5.3",
    "description": "A single authenticated action exposes the remote VPS root shell. It can use or install software as needed, read/write host files, run programs, manage services, call APIs, and compose arbitrary non-interactive workflows. Named tools are examples, never a capability boundary."
  },
  "servers": [
    {
      "url": "__SERVER_URL__"
    }
  ],
  "security": [
    {
      "BearerAuth": []
    }
  ],
  "components": {
    "securitySchemes": {
      "BearerAuth": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "opaque"
      }
    },
    "schemas": {
      "CommandInput": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "command": {
            "type": "string",
            "description": "Any shell command or multiline script to execute with /bin/bash -lc. Multiple dependent steps may be combined into one script."
          },
          "workdir": {
            "type": "string",
            "description": "Optional absolute or relative host working directory. Defaults to /root. It is not restricted to a repository workspace."
          },
          "stdin": {
            "type": "string",
            "description": "Optional standard input passed to the command. Useful for non-interactive CLI input, scripts, payloads and file content."
          },
          "timeout_seconds": {
            "type": "integer",
            "minimum": 1,
            "description": "Optional per-workflow timeout in seconds. It may shorten but cannot exceed the server-side COMMAND_TIMEOUT_SECONDS limit."
          }
        },
        "required": [
          "command"
        ]
      },
      "CommandJob": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string"
          },
          "status": {
            "type": "string",
            "enum": [
              "running",
              "completed",
              "failed",
              "cancelled",
              "timed_out"
            ],
            "description": "completed means exit code 0; a non-zero command exit is failed. Cancellation and timeout have distinct terminal states."
          },
          "workdir": {
            "type": "string"
          },
          "exit_code": {
            "type": [
              "integer",
              "null"
            ],
            "description": "Null while running; process exit code after completion."
          },
          "stdout": {
            "type": "string",
            "description": "Current rolling stdout tail, available while running."
          },
          "stderr": {
            "type": "string",
            "description": "Current rolling stderr tail, available while running."
          },
          "timed_out": {
            "type": "boolean"
          },
          "truncated": {
            "type": "boolean",
            "description": "True when stored output or the requested tail omits older output."
          },
          "duration_ms": {
            "type": "integer",
            "minimum": 0
          },
          "error": {
            "type": "string"
          },
          "started_at": {
            "type": "string",
            "format": "date-time"
          },
          "finished_at": {
            "type": "string",
            "format": "date-time"
          },
          "revision": {
            "type": "integer",
            "minimum": 1,
            "description": "Monotonic job revision. Pass the last seen value as after to wait for the next change."
          }
        },
        "required": [
          "id",
          "status",
          "workdir",
          "revision",
          "exit_code",
          "stdout",
          "stderr",
          "timed_out",
          "truncated",
          "duration_ms",
          "started_at"
        ]
      }
    }
  },
  "paths": {
    "/v1/command/run": {
      "post": {
        "operationId": "runCommand",
        "x-openai-isConsequential": false,
        "summary": "Run a root shell workflow on the remote VPS",
        "description": "Runs any non-interactive root shell workflow via /bin/bash -lc; installed tools are not a capability boundary. Use for short commands. Cloudflare currently documents a 125-second default Proxy Read Timeout; prefer startCommand for long work to avoid HTTP 524.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CommandInput"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Workflow finished. Inspect exit_code, stdout, stderr, timed_out and truncated before deciding the next step."
          },
          "400": {
            "description": "Invalid request."
          },
          "401": {
            "description": "Missing or invalid Bearer token."
          },
          "500": {
            "description": "The shell could not be started or the requested workdir is unavailable."
          }
        }
      }
    },
    "/v1/command/start": {
      "post": {
        "operationId": "startCommand",
        "x-openai-isConsequential": false,
        "summary": "Start a long-running root shell workflow",
        "description": "Starts a root shell workflow independently of the HTTP request and immediately returns a job id. Prefer for long work; observe it with revision-based getCommandJob long polling.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CommandInput"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Job accepted. Use getCommandJob to poll until it reaches a terminal state."
          },
          "400": {
            "description": "Invalid request."
          },
          "401": {
            "description": "Missing or invalid Bearer token."
          }
        }
      }
    },
    "/v1/command/jobs/{id}": {
      "get": {
        "operationId": "getCommandJob",
        "x-openai-isConsequential": false,
        "summary": "Get an asynchronous command job",
        "description": "Returns a flat job snapshot with live rolling stdout/stderr. For event-driven observation, pass the last revision as after; the request returns immediately on output/status change or after a short wait timeout.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "after",
            "in": "query",
            "required": false,
            "description": "Last revision already seen. If it still matches a running job, wait for a change instead of polling immediately.",
            "schema": {
              "type": "integer",
              "minimum": 0
            }
          },
          {
            "name": "wait_seconds",
            "in": "query",
            "required": false,
            "description": "Long-poll wait when after matches. Defaults to 10 seconds when after is supplied; maximum 20 seconds. Zero returns immediately.",
            "schema": {
              "type": "integer",
              "minimum": 0,
              "maximum": 20
            }
          },
          {
            "name": "tail_chars",
            "in": "query",
            "required": false,
            "description": "Return only the newest N Unicode characters from each output stream. Long-poll requests default to 12000; ordinary snapshots return the full stored rolling output.",
            "schema": {
              "type": "integer",
              "minimum": 1
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Current job snapshot. A matching running revision waits for output/status change or the requested heartbeat timeout.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CommandJob"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid Bearer token."
          },
          "404": {
            "description": "Job not found."
          },
          "400": {
            "description": "Invalid after, wait_seconds, or tail_chars."
          }
        }
      }
    },
    "/v1/command/jobs/{id}/cancel": {
      "post": {
        "operationId": "cancelCommandJob",
        "x-openai-isConsequential": false,
        "summary": "Cancel an asynchronous command job",
        "description": "Requests cancellation of a running job and its process group. Completed jobs are left unchanged.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "202": {
            "description": "Cancellation request accepted."
          },
          "401": {
            "description": "Missing or invalid Bearer token."
          },
          "404": {
            "description": "Job not found."
          }
        }
      }
    }
  }
}
